import React from 'react'
import { storiesOf } from '@storybook/react'
import { CardLoading } from 'instruments'

storiesOf('Instruments|Cards.Loading', module).add('No props', () => (
  <CardLoading />
))
