import React from 'react'
import { storiesOf } from '@storybook/react'
import { Info } from 'instruments'

storiesOf('Instruments|Forms.Info', module).add('Default', () => (
  <Info>This is a little bit of info for you</Info>
))
