import React from 'react'
import { storiesOf } from '@storybook/react'
import { FormLabel } from 'instruments'

storiesOf('Instruments|Forms.FormLabel', module).add('Default', () => (
  <FormLabel>Mock Label</FormLabel>
))
