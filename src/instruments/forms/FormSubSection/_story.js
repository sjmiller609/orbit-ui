import React from 'react'
import { storiesOf } from '@storybook/react'
import { FormSubSection } from 'instruments'

storiesOf('Instruments|Forms.FormSubSection', module).add('Default', () => (
  <FormSubSection title="Mock Form Section">
    [Replace with form parts]
  </FormSubSection>
))
